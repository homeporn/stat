# Инструкции по деплою

## 🚀 Вариант 1: Railway (Рекомендуется)

Railway отлично подходит для приложений с SQLite, так как предоставляет постоянное хранилище.

### Шаги:

1. Зарегистрируйтесь на [railway.app](https://railway.app)
2. Создайте новый проект
3. Подключите GitHub репозиторий
4. Railway автоматически определит Next.js и запустит деплой
5. **Важно**: Добавьте Volume (постоянное хранилище) для папки `/data` в настройках сервиса
6. Добавьте переменную окружения (опционально):
   - `DATABASE_URL=file:/data/dev.db` (по умолчанию используется `/data/dev.db` в production)
   - `NODE_ENV=production`

### Примечания:
- Файлы `railway.json` и `nixpacks.toml` уже настроены для работы с `better-sqlite3`
- Volume для `/data` необходим для сохранения базы данных между деплоями
- Если деплой не проходит, проверьте логи сборки - может потребоваться установка дополнительных зависимостей

### Преимущества:
- ✅ Поддержка SQLite с постоянным хранилищем
- ✅ Автоматический деплой из GitHub
- ✅ Простая настройка
- ✅ Бесплатный тариф для начала

---

## 🌐 Вариант 2: Render

Render также поддерживает постоянное хранилище для SQLite.

### Шаги:

1. Зарегистрируйтесь на [render.com](https://render.com)
2. Создайте новый Web Service
3. Подключите GitHub репозиторий
4. Настройки:
   - **Build Command**: `npm install && npm run build`
   - **Start Command**: `npm start`
   - **Environment**: Node
5. Добавьте переменную окружения:
   - `DATABASE_URL=file:./data/dev.db`

### Преимущества:
- ✅ Бесплатный тариф
- ✅ Постоянное хранилище
- ✅ Автоматический деплой

---

## 🪂 Вариант 3: Fly.io

Хороший вариант для контейнерных приложений.

### Шаги:

1. Установите Fly CLI: `curl -L https://fly.io/install.sh | sh`
2. Зарегистрируйтесь: `fly auth signup`
3. Создайте `fly.toml` (см. ниже)
4. Деплой: `fly deploy`

### Преимущества:
- ✅ Глобальная сеть
- ✅ Хорошая производительность
- ✅ Постоянные тома для БД

---

## ⚡ Вариант 4: Vercel (НЕ рекомендуется с SQLite)

Vercel - лучший вариант для Next.js, но **SQLite не поддерживается** в serverless окружении Vercel, так как:
- Файловая система эфемерная (файлы теряются при каждом деплое)
- Нет постоянного хранилища для БД

### Если хотите использовать Vercel:
Нужно перейти на PostgreSQL или другую облачную БД:
1. Создайте базу данных PostgreSQL (например, на [Supabase](https://supabase.com) или [Neon](https://neon.tech))
2. Перепишите `lib/db.ts` для работы с PostgreSQL через `pg` или `@vercel/postgres`
3. Деплой на Vercel через GitHub

### Преимущества:
- ✅ Лучшая производительность для Next.js
- ✅ Автоматический деплой
- ✅ CDN и оптимизация
- ✅ Бесплатный тариф

### Недостатки:
- ❌ Требует переписывания кода БД
- ❌ Дополнительные расходы на облачную БД

---

## 🖥️ Вариант 5: Self-hosted (VPS)

Для полного контроля можно использовать VPS (DigitalOcean, Hetzner, и т.д.).

### Шаги:

1. Арендуйте VPS (минимум 1GB RAM)
2. Установите Node.js и PM2
3. Клонируйте репозиторий
4. Настройте Nginx как reverse proxy
5. Настройте SSL (Let's Encrypt)

### Преимущества:
- ✅ Полный контроль
- ✅ Низкая стоимость
- ✅ Гибкость

---

## 📝 Общие рекомендации

### Перед деплоем:

1. **Проверьте переменные окружения:**
   - `DATABASE_URL` - путь к базе данных
   - `NODE_ENV=production` - для production

2. **Скрипты сборки уже настроены:**
   ```json
   {
     "scripts": {
       "build": "next build",
       "start": "next start"
     }
   }
   ```

3. **Добавьте `.env.example`:**
   ```
   DATABASE_URL="file:./dev.db"
   NODE_ENV=production
   ```

4. **Проверьте `.gitignore`:**
   - Убедитесь, что `.env` и `*.db` не коммитятся

### После деплоя:

1. Проверьте работу API endpoints
2. Проверьте создание/чтение данных
3. Настройте мониторинг (опционально)

---

## 🎯 Рекомендация

Для вашего приложения **лучше всего подходит Railway**, так как:
- Простая настройка
- Поддержка SQLite из коробки
- Автоматический деплой
- Бесплатный тариф для начала

Если нужна максимальная производительность и масштабируемость - перейдите на **Vercel + PostgreSQL**.

